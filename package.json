{
  "name": "png2icns",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based PNG to macOS ICNS icon converter",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
